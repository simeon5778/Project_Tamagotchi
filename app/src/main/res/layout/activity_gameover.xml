<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.tamagotchi.project_tamagotchi.Gameover">

    <ImageView
        android:id="@+id/tombstone_id"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_above="@+id/shareButton"
        android:layout_marginBottom="34dp"
        android:layout_toEndOf="@+id/ghost_id"
        app:srcCompat="@drawable/tombstone" />

    <ImageView
        android:id="@+id/ghost_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/tombstone_id"
        android:layout_centerHorizontal="true"
        android:background="@drawable/ghost" />

    <TextView
        android:id="@+id/finishText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="TextView" />

    <Button
        android:id="@+id/mainmenuButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="15dp"
        android:layout_marginStart="14dp"
        android:background="@drawable/buttonshape"
        android:onClick="goToMainMenu"
        android:text="MAIN MENU" />

    <Button
        android:id="@+id/shareButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/mainmenuButton"
        android:layout_alignBottom="@+id/mainmenuButton"
        android:layout_toEndOf="@+id/mainmenuButton"
        android:background="@drawable/buttonshape"
        android:onClick="shareOnFb"
        android:text="SHARE" />
</RelativeLayout>
